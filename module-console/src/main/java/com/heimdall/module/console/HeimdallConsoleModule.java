package com.heimdall.module.console;

/**
 * Phase 0 skeleton for the Console module.
 *
 * <p>Carries no behaviour yet — it exists so the module boundary, its bytecode level and the
 * conformance rules that police it are all in place before any feature code is written.
 */
public final class HeimdallConsoleModule {

    /** The module's stable identifier, used for config keys and logging. */
    public static final String ID = "console";

    private HeimdallConsoleModule() {
    }
}
