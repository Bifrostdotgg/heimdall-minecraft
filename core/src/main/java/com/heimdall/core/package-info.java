/**
 * Platform-free core of the Heimdall plugin.
 *
 * <p>Nothing in this package (or below it) may reference a server platform API — no Bukkit, Paper,
 * Velocity or BungeeCord types. The {@code :conformance} module enforces that with ArchUnit.
 */
package com.heimdall.core;
