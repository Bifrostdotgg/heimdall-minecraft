/**
 * Small, dependency-free helpers shared by the rest of core.
 *
 * <p>Nothing here knows about HTTP, the mirror or a server platform — if a helper needs any of
 * those it belongs in the package that owns them, not here.
 */
package com.heimdall.core.util;
