/**
 * The plugin's <em>local</em> configuration — and nothing else.
 *
 * <p>Exactly one file lives here: {@code bootstrap.yml}, holding only what the plugin needs in
 * order to reach the bot and prove who it is. Every other setting arrives from the dashboard over
 * the tunnel as remote config. See {@link com.heimdall.core.config.BootstrapConfig} for why that
 * split is enforced rather than merely encouraged.
 */
package com.heimdall.core.config;
